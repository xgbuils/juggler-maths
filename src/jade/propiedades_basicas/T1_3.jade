extends ../templates/theorem.jade

block theorem
  p Dado un patrón malabar $f$, $n \in \Nat$ y $m \in \Nat$ tal que $m = f'^{-1}(n)$
  p si $f(n) \gt 0$, entonces $f(f'^{-1}(n)) \gt 0$

block demonstration
  p La proposición del teorema también se puede expresar como:
  p Si $f(f'(m)) \gt 0$, entonces $f(m) \gt 0$
  p Esto se corresponde al #[em sólo si] del #[a(href="#T1.2") teorema 1.2]. Por tanto queda demostrado.